---

- name: "extract artifact"
  unarchive:
    src: artifact.tar.gz
    dest: /home/ubuntu

- name: "start application"
  become: yes
  shell: |
    cd ~/backend
    npm install
    npm run build
    pm2 stop default
    pm2 start npm -- start